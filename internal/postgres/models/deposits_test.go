// Code generated by SQLBoiler 3.7.1 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/randomize"
	"github.com/volatiletech/sqlboiler/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testDeposits(t *testing.T) {
	t.Parallel()

	query := Deposits()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testDepositsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Deposit{}
	if err = randomize.Struct(seed, o, depositDBTypes, true, depositColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deposit struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Deposits().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDepositsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Deposit{}
	if err = randomize.Struct(seed, o, depositDBTypes, true, depositColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deposit struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := Deposits().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Deposits().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDepositsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Deposit{}
	if err = randomize.Struct(seed, o, depositDBTypes, true, depositColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deposit struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := DepositSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Deposits().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDepositsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Deposit{}
	if err = randomize.Struct(seed, o, depositDBTypes, true, depositColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deposit struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := DepositExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if Deposit exists: %s", err)
	}
	if !e {
		t.Errorf("Expected DepositExists to return true, but got false.")
	}
}

func testDepositsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Deposit{}
	if err = randomize.Struct(seed, o, depositDBTypes, true, depositColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deposit struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	depositFound, err := FindDeposit(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if depositFound == nil {
		t.Error("want a record, got nil")
	}
}

func testDepositsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Deposit{}
	if err = randomize.Struct(seed, o, depositDBTypes, true, depositColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deposit struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = Deposits().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testDepositsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Deposit{}
	if err = randomize.Struct(seed, o, depositDBTypes, true, depositColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deposit struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := Deposits().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testDepositsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	depositOne := &Deposit{}
	depositTwo := &Deposit{}
	if err = randomize.Struct(seed, depositOne, depositDBTypes, false, depositColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deposit struct: %s", err)
	}
	if err = randomize.Struct(seed, depositTwo, depositDBTypes, false, depositColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deposit struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = depositOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = depositTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := Deposits().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testDepositsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	depositOne := &Deposit{}
	depositTwo := &Deposit{}
	if err = randomize.Struct(seed, depositOne, depositDBTypes, false, depositColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deposit struct: %s", err)
	}
	if err = randomize.Struct(seed, depositTwo, depositDBTypes, false, depositColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deposit struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = depositOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = depositTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Deposits().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testDepositsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Deposit{}
	if err = randomize.Struct(seed, o, depositDBTypes, true, depositColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deposit struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Deposits().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testDepositsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Deposit{}
	if err = randomize.Struct(seed, o, depositDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Deposit struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(depositColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := Deposits().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testDepositToManySubscriptions(t *testing.T) {
	var err error
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Deposit
	var b, c Subscription

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, depositDBTypes, true, depositColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deposit struct: %s", err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = randomize.Struct(seed, &b, subscriptionDBTypes, false, subscriptionColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, subscriptionDBTypes, false, subscriptionColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}

	b.DepositID = a.ID
	c.DepositID = a.ID

	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := a.Subscriptions().All(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	bFound, cFound := false, false
	for _, v := range check {
		if v.DepositID == b.DepositID {
			bFound = true
		}
		if v.DepositID == c.DepositID {
			cFound = true
		}
	}

	if !bFound {
		t.Error("expected to find b")
	}
	if !cFound {
		t.Error("expected to find c")
	}

	slice := DepositSlice{&a}
	if err = a.L.LoadSubscriptions(ctx, tx, false, (*[]*Deposit)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.Subscriptions); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	a.R.Subscriptions = nil
	if err = a.L.LoadSubscriptions(ctx, tx, true, &a, nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.Subscriptions); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	if t.Failed() {
		t.Logf("%#v", check)
	}
}

func testDepositToManyAddOpSubscriptions(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Deposit
	var b, c, d, e Subscription

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, depositDBTypes, false, strmangle.SetComplement(depositPrimaryKeyColumns, depositColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*Subscription{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, subscriptionDBTypes, false, strmangle.SetComplement(subscriptionPrimaryKeyColumns, subscriptionColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	foreignersSplitByInsertion := [][]*Subscription{
		{&b, &c},
		{&d, &e},
	}

	for i, x := range foreignersSplitByInsertion {
		err = a.AddSubscriptions(ctx, tx, i != 0, x...)
		if err != nil {
			t.Fatal(err)
		}

		first := x[0]
		second := x[1]

		if a.ID != first.DepositID {
			t.Error("foreign key was wrong value", a.ID, first.DepositID)
		}
		if a.ID != second.DepositID {
			t.Error("foreign key was wrong value", a.ID, second.DepositID)
		}

		if first.R.Deposit != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}
		if second.R.Deposit != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}

		if a.R.Subscriptions[i*2] != first {
			t.Error("relationship struct slice not set to correct value")
		}
		if a.R.Subscriptions[i*2+1] != second {
			t.Error("relationship struct slice not set to correct value")
		}

		count, err := a.Subscriptions().Count(ctx, tx)
		if err != nil {
			t.Fatal(err)
		}
		if want := int64((i + 1) * 2); count != want {
			t.Error("want", want, "got", count)
		}
	}
}
func testDepositToOneClassUsingClass(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local Deposit
	var foreign Class

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, depositDBTypes, false, depositColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deposit struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, classDBTypes, false, classColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Class struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.ClassID = foreign.ID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Class().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.ID != foreign.ID {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := DepositSlice{&local}
	if err = local.L.LoadClass(ctx, tx, false, (*[]*Deposit)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Class == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Class = nil
	if err = local.L.LoadClass(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Class == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testDepositToOnePeriodUsingPeriod(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local Deposit
	var foreign Period

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, depositDBTypes, true, depositColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deposit struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, periodDBTypes, false, periodColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Period struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&local.PeriodID, foreign.ID)
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Period().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if !queries.Equal(check.ID, foreign.ID) {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := DepositSlice{&local}
	if err = local.L.LoadPeriod(ctx, tx, false, (*[]*Deposit)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Period == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Period = nil
	if err = local.L.LoadPeriod(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Period == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testDepositToOneStudentUsingStudent(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local Deposit
	var foreign Student

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, depositDBTypes, false, depositColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deposit struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, studentDBTypes, false, studentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Student struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.StudentID = foreign.ID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Student().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.ID != foreign.ID {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := DepositSlice{&local}
	if err = local.L.LoadStudent(ctx, tx, false, (*[]*Deposit)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Student == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Student = nil
	if err = local.L.LoadStudent(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Student == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testDepositToOneSubjectUsingSubject(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local Deposit
	var foreign Subject

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, depositDBTypes, false, depositColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deposit struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, subjectDBTypes, false, subjectColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Subject struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.SubjectID = foreign.ID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Subject().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.ID != foreign.ID {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := DepositSlice{&local}
	if err = local.L.LoadSubject(ctx, tx, false, (*[]*Deposit)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Subject == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Subject = nil
	if err = local.L.LoadSubject(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Subject == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testDepositToOneSetOpClassUsingClass(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Deposit
	var b, c Class

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, depositDBTypes, false, strmangle.SetComplement(depositPrimaryKeyColumns, depositColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, classDBTypes, false, strmangle.SetComplement(classPrimaryKeyColumns, classColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, classDBTypes, false, strmangle.SetComplement(classPrimaryKeyColumns, classColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Class{&b, &c} {
		err = a.SetClass(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Class != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.Deposits[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.ClassID != x.ID {
			t.Error("foreign key was wrong value", a.ClassID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.ClassID))
		reflect.Indirect(reflect.ValueOf(&a.ClassID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.ClassID != x.ID {
			t.Error("foreign key was wrong value", a.ClassID, x.ID)
		}
	}
}
func testDepositToOneSetOpPeriodUsingPeriod(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Deposit
	var b, c Period

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, depositDBTypes, false, strmangle.SetComplement(depositPrimaryKeyColumns, depositColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, periodDBTypes, false, strmangle.SetComplement(periodPrimaryKeyColumns, periodColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, periodDBTypes, false, strmangle.SetComplement(periodPrimaryKeyColumns, periodColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Period{&b, &c} {
		err = a.SetPeriod(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Period != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.Deposits[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if !queries.Equal(a.PeriodID, x.ID) {
			t.Error("foreign key was wrong value", a.PeriodID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.PeriodID))
		reflect.Indirect(reflect.ValueOf(&a.PeriodID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if !queries.Equal(a.PeriodID, x.ID) {
			t.Error("foreign key was wrong value", a.PeriodID, x.ID)
		}
	}
}

func testDepositToOneRemoveOpPeriodUsingPeriod(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Deposit
	var b Period

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, depositDBTypes, false, strmangle.SetComplement(depositPrimaryKeyColumns, depositColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, periodDBTypes, false, strmangle.SetComplement(periodPrimaryKeyColumns, periodColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err = a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = a.SetPeriod(ctx, tx, true, &b); err != nil {
		t.Fatal(err)
	}

	if err = a.RemovePeriod(ctx, tx, &b); err != nil {
		t.Error("failed to remove relationship")
	}

	count, err := a.Period().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 0 {
		t.Error("want no relationships remaining")
	}

	if a.R.Period != nil {
		t.Error("R struct entry should be nil")
	}

	if !queries.IsValuerNil(a.PeriodID) {
		t.Error("foreign key value should be nil")
	}

	if len(b.R.Deposits) != 0 {
		t.Error("failed to remove a from b's relationships")
	}
}

func testDepositToOneSetOpStudentUsingStudent(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Deposit
	var b, c Student

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, depositDBTypes, false, strmangle.SetComplement(depositPrimaryKeyColumns, depositColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, studentDBTypes, false, strmangle.SetComplement(studentPrimaryKeyColumns, studentColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, studentDBTypes, false, strmangle.SetComplement(studentPrimaryKeyColumns, studentColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Student{&b, &c} {
		err = a.SetStudent(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Student != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.Deposits[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.StudentID != x.ID {
			t.Error("foreign key was wrong value", a.StudentID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.StudentID))
		reflect.Indirect(reflect.ValueOf(&a.StudentID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.StudentID != x.ID {
			t.Error("foreign key was wrong value", a.StudentID, x.ID)
		}
	}
}
func testDepositToOneSetOpSubjectUsingSubject(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Deposit
	var b, c Subject

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, depositDBTypes, false, strmangle.SetComplement(depositPrimaryKeyColumns, depositColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, subjectDBTypes, false, strmangle.SetComplement(subjectPrimaryKeyColumns, subjectColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, subjectDBTypes, false, strmangle.SetComplement(subjectPrimaryKeyColumns, subjectColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Subject{&b, &c} {
		err = a.SetSubject(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Subject != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.Deposits[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.SubjectID != x.ID {
			t.Error("foreign key was wrong value", a.SubjectID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.SubjectID))
		reflect.Indirect(reflect.ValueOf(&a.SubjectID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.SubjectID != x.ID {
			t.Error("foreign key was wrong value", a.SubjectID, x.ID)
		}
	}
}

func testDepositsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Deposit{}
	if err = randomize.Struct(seed, o, depositDBTypes, true, depositColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deposit struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testDepositsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Deposit{}
	if err = randomize.Struct(seed, o, depositDBTypes, true, depositColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deposit struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := DepositSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testDepositsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Deposit{}
	if err = randomize.Struct(seed, o, depositDBTypes, true, depositColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deposit struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := Deposits().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	depositDBTypes = map[string]string{`ID`: `uuid`, `StudentID`: `uuid`, `Amount`: `integer`, `Ref`: `character varying`, `Status`: `character varying`, `Channel`: `character varying`, `CreatedAt`: `timestamp without time zone`, `UpdatedAt`: `timestamp without time zone`, `SubjectID`: `uuid`, `DaysOfWeek`: `integer`, `ClassID`: `uuid`, `PaymentRef`: `character varying`, `PeriodID`: `uuid`}
	_              = bytes.MinRead
)

func testDepositsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(depositPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(depositAllColumns) == len(depositPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &Deposit{}
	if err = randomize.Struct(seed, o, depositDBTypes, true, depositColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deposit struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Deposits().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, depositDBTypes, true, depositPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Deposit struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testDepositsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(depositAllColumns) == len(depositPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &Deposit{}
	if err = randomize.Struct(seed, o, depositDBTypes, true, depositColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deposit struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Deposits().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, depositDBTypes, true, depositPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Deposit struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(depositAllColumns, depositPrimaryKeyColumns) {
		fields = depositAllColumns
	} else {
		fields = strmangle.SetComplement(
			depositAllColumns,
			depositPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := DepositSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testDepositsUpsert(t *testing.T) {
	t.Parallel()

	if len(depositAllColumns) == len(depositPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := Deposit{}
	if err = randomize.Struct(seed, &o, depositDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Deposit struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert Deposit: %s", err)
	}

	count, err := Deposits().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, depositDBTypes, false, depositPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Deposit struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert Deposit: %s", err)
	}

	count, err = Deposits().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
